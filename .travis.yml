branches:
  only:
    - master
    - develop
    - travisci

language: c

install:
  - sudo apt-get install mono-devel mono-gmcs nunit-console

script:
  - make
  - nunit-console ./CorrugatedIron.Tests/bin/Debug/CorrugatedIron.Tests.dll
